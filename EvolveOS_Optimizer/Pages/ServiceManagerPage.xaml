<Page
    x:Class="EvolveOS_Optimizer.Pages.ServiceManagerPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:helpers="using:EvolveOS_Optimizer.Utilities.Helpers"
    mc:Ignorable="d">

    <Grid x:Name="ContentArea" Padding="4,14,24,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid x:Name="SummaryPanel" Grid.Row="0" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="6" 
                    Padding="16,12" 
                    Margin="0,0,8,0">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE9F5;" FontSize="24" Foreground="{ThemeResource MyDynamicAccentBrush}"/>
                    <StackPanel>
                        <TextBlock x:Name="TotalServicesText" Text="0" FontFamily="{ThemeResource Jura-SemiBold}" FontSize="18" />
                        <TextBlock helpers:Loc.Key="service_manager_page_total_services" FontFamily="{ThemeResource Jura}" FontSize="12" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="6" 
                    Padding="16,12" 
                    Margin="4,0">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE768;" FontSize="24" Foreground="{ThemeResource SystemFillColorSuccessBrush}"/>
                    <StackPanel>
                        <TextBlock x:Name="RunningServicesText" Text="0" FontFamily="{ThemeResource Jura-SemiBold}" FontSize="18" />
                        <TextBlock helpers:Loc.Key="service_manager_page_running_services" FontFamily="{ThemeResource Jura}" FontSize="12" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" 
                    Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                    BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}" 
                    BorderThickness="1" 
                    CornerRadius="6" 
                    Padding="16,12" 
                    Margin="8,0,0,0">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <FontIcon Glyph="&#xE71A;" FontSize="24" Foreground="{ThemeResource SystemFillColorCautionBrush}"/>
                    <StackPanel>
                        <TextBlock x:Name="StoppedServicesText" FontFamily="{ThemeResource Jura-SemiBold}" Text="0" FontSize="18" />
                        <TextBlock helpers:Loc.Key="service_manager_page_stopped_services" FontFamily="{ThemeResource Jura}" FontSize="12" Opacity="0.7"/>
                    </StackPanel>
                </StackPanel>
            </Border>
        </Grid>

        <Grid Grid.Row="1" Margin="0,0,0,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <AutoSuggestBox x:Name="SearchBox"
                            PlaceholderText="{helpers:ResourceString Key=service_manager_page_search_placeholder}"
                            Width="300"
                            QueryIcon="Find"
                            HorizontalAlignment="Left"
                            TextChanged="SearchBox_TextChanged"/>

            <ComboBox x:Name="FilterComboBox"
                      Grid.Column="1"
                      Margin="8,0,0,0"
                      Width="140"
                      FontFamily="{ThemeResource Jura}"
                      SelectionChanged="FilterComboBox_SelectionChanged">
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_all}" IsSelected="True"/>
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_running}"/>
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_stopped}"/>
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_automatic}"/>
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_manual}"/>
                <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_disabled}"/>
            </ComboBox>

            <Button x:Name="LiveMonitoringButton" 
                    Grid.Column="2" 
                    Margin="8,0,0,0" 
                    MinWidth="200"
                    HorizontalContentAlignment="Center"
                    Click="LiveMonitoringButton_Click">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon x:Name="LiveMonitoringIcon" Glyph="&#xE769;" FontSize="14" Foreground="LimeGreen"/>
                    <TextBlock x:Name="LiveMonitoringText" 
                               FontFamily="{ThemeResource Jura}" 
                               helpers:Loc.Key="process_manager_page_live_monitor"/>
                </StackPanel>
            </Button>
        </Grid>

        <TextBlock x:Name="ResultsText" 
                   Grid.Row="2" 
                   FontSize="12" 
                   Opacity="0.7" 
                   Margin="0,0,0,8"/>

        <Grid x:Name="HeaderGrid" 
              Grid.Row="3" 
              Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}" 
              Padding="12,8" 
              CornerRadius="4" 
              Margin="0,0,0,8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.8*"/>
                <ColumnDefinition Width="0.8*"/>
                <ColumnDefinition Width="0.9*"/>
                <ColumnDefinition Width="1.2*"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="NameHeader" 
                    Tag="Name" 
                    Click="SortHeader_Click" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Padding="0" 
                    HorizontalAlignment="Left">
                <TextBlock helpers:Loc.Key="service_manager_page_service_name"
                           FontFamily="{ThemeResource Jura-SemiBold}"
                           FontSize="13"/>
            </Button>

            <Button x:Name="StatusHeader" 
                    Tag="Status" 
                    Grid.Column="1" 
                    Click="SortHeader_Click" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Padding="0" 
                    HorizontalAlignment="Center">
                <TextBlock helpers:Loc.Key="service_manager_page_status"
                           FontFamily="{ThemeResource Jura-SemiBold}"
                           FontSize="13"/>
            </Button>

            <Button x:Name="StartTypeHeader" 
                    Tag="StartType" 
                    Grid.Column="2" 
                    Click="SortHeader_Click" 
                    Background="Transparent" 
                    BorderThickness="0" 
                    Padding="0" 
                    HorizontalAlignment="Center">
                <TextBlock helpers:Loc.Key="service_manager_page_startup"
                           FontWeight="SemiBold"
                           FontSize="13"/>
            </Button>

            <TextBlock helpers:Loc.Key="service_manager_page_actions" 
                       Grid.Column="3"
                       FontFamily="{ThemeResource Jura}"
                       FontSize="13"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Grid>

        <Grid Grid.Row="4" Margin="0,0,0,24">
            <ProgressRing x:Name="LoadingRing" 
                          IsActive="True"
                          Width="48"
                          Height="48"
                          HorizontalAlignment="Center" 
                          VerticalAlignment="Center"/>

            <ListView x:Name="ServicesListView" 
                      SelectionMode="None"
                      Visibility="Collapsed">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                                CornerRadius="4"
                                Padding="12,10"
                                Margin="0,2">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="1.8*"/>
                                    <ColumnDefinition Width="0.8*"/>
                                    <ColumnDefinition Width="0.9*"/>
                                    <ColumnDefinition Width="1.2*"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Orientation="Horizontal" Spacing="8">
                                    <FontIcon Glyph="{Binding StatusDisplay.Glyph}" 
                                              FontSize="14" 
                                              Foreground="{Binding StatusDisplay.Color}" 
                                              Opacity="0.8"/>
                                    <StackPanel Spacing="2">
                                        <TextBlock Text="{Binding DisplayName}" TextTrimming="CharacterEllipsis" FontSize="13"/>
                                        <TextBlock Text="{Binding Name}" FontSize="11" Opacity="0.5" TextTrimming="CharacterEllipsis"/>
                                    </StackPanel>
                                </StackPanel>

                                <Border Grid.Column="1"
                                        Background="{ThemeResource SubtleFillColorSecondaryBrush}"
                                        CornerRadius="4"
                                        Padding="10,4"
                                        HorizontalAlignment="Center">
                                    <TextBlock Text="{Binding Status}" FontSize="12" Opacity="0.85" VerticalAlignment="Center"/>
                                </Border>

                                <ComboBox Grid.Column="2"
                                          SelectedIndex="{Binding StartTypeIndex}"
                                          Tag="{Binding Name}"
                                          FontFamily="{ThemeResource Jura}"
                                          SelectionChanged="StartupType_SelectionChanged"
                                          DropDownOpened="StartupType_DropDownOpened"
                                          DropDownClosed="StartupType_DropDownClosed"
                                          HorizontalAlignment="Center"
                                          MinWidth="140"
                                          Padding="8,4">
                                    <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_automatic}"/>
                                    <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_manual}"/>
                                    <ComboBoxItem Content="{helpers:ResourceString Key=service_manager_page_disabled}"/>
                                </ComboBox>

                                <StackPanel Grid.Column="3" Orientation="Horizontal" HorizontalAlignment="Center" Spacing="4">
                                    <Button Tag="{Binding Name}"
                                            Click="StartService_Click"
                                            ToolTipService.ToolTip="{helpers:ResourceString Key=service_manager_page_start_tooltip}"
                                            Padding="8,6"
                                            IsEnabled="{Binding CanStart}">
                                        <FontIcon Glyph="&#xE768;" FontSize="12"/>
                                    </Button>

                                    <Button Tag="{Binding Name}"
                                            Click="StopService_Click"
                                            ToolTipService.ToolTip="{helpers:ResourceString Key=service_manager_page_stop_tooltip}"
                                            Padding="8,6"
                                            IsEnabled="{Binding CanStop}">
                                        <FontIcon Glyph="&#xE71A;" FontSize="12"/>
                                    </Button>

                                    <Button Tag="{Binding Name}"
                                            Click="RestartService_Click"
                                            ToolTipService.ToolTip="{helpers:ResourceString Key=service_manager_page_restart_tooltip}"
                                            Padding="8,6"
                                            IsEnabled="{Binding CanStop}">
                                        <FontIcon Glyph="&#xE72C;" FontSize="12"/>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
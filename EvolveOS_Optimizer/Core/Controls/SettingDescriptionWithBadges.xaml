<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="EvolveOS_Optimizer.Core.Controls.SettingDescriptionWithBadges"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:ctk="using:CommunityToolkit.WinUI.Controls"
    HorizontalAlignment="Stretch"
    HorizontalContentAlignment="Stretch">
    <StackPanel Spacing="6">
        <TextBlock
            Text="{x:Bind DescriptionText, Mode=OneWay}"
            FontFamily="{ThemeResource AppCustomFont}"
            TextWrapping="Wrap"
            Style="{StaticResource CaptionTextBlockStyle}"/>

        <ItemsControl
            ItemsSource="{x:Bind BadgeRow, Mode=OneWay}"
            Visibility="{x:Bind ShowBadges, Mode=OneWay,
                         Converter={StaticResource BooleanToVisibilityConverter}}"
            ItemTemplateSelector="{StaticResource BadgePillTemplateSelector}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <ctk:WrapPanel Orientation="Horizontal"
                                   HorizontalSpacing="6"
                                   VerticalSpacing="4"/>
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
        </ItemsControl>
    </StackPanel>
</UserControl>
